fastapi==0.115.0
uvicorn[standard]==0.32.0
anthropic==0.39.0
httpx<0.28
python-dotenv==1.0.1
pydantic==2.9.2
# P3 semantic ranking blend (bge-small-en-v1.5 via ONNX runtime, no PyTorch)
fastembed==0.8.0
numpy>=1.26,<3
# Auth + persistence (SQLite is stdlib; only need crypto + JWT + email parsing)
bcrypt>=4.0,<5
pyjwt>=2.8,<3
email-validator>=2.0,<3
